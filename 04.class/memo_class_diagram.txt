-Memo
  -readlineManager
  -memoDatabase
  -memoOption(process.argv)
  -memoSelector
  -run()
  -#createPipeline()
  -#createQuestion()
  -#list()
  -#read()
  -#delete()

-MemoDatabase
  -db
  -close()
  -#createTable()
  -getMemoData()
  -saveMemo(title, content)
  -deleteMemo(id)

-MemoOption {
  -argv
  -parseOptions()

-MemoSelector {
  -memoDatabase
  -select(message, choices)
  -getSelectedMemo(message)

-ReadlineManager {
  -rl
  -close()
  -getPipelineText()
  -rlQuestion(prompt)
